{
  "name": "autocomplete-for-deno",
  "main": "./lib/main",
  "version": "0.1.0",
  "description": "autocomplete provider for Atom for Deno.js.",
  "keywords": [
    "autocomplete",
    "provider",
    "boilerplate"
  ],
  "engines": {
    "atom": ">=1.15.0"
  },
  "providedServices": {
    "autocomplete.provider": {
      "versions": {
        "2.0.0": "getProvider"
      }
    }
  }
}
